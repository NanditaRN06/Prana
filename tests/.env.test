# Test Environment Variables
NODE_ENV=test
PORT=5001
JWT_SECRET=prana-test-secret-key-12345
LOG_LEVEL=error

# Database
# Note: MONGODB_URI is dynamically set by MongoMemoryServer in globalSetup.js
# but we provide a placeholder if needed.
MONGODB_URI=mongodb://localhost:27017/prana-test
